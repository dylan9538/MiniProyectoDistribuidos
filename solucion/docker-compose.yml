version: '2'


services:
  mirror_c:
    image: mirror
    environment:
      - deps="postgresql,python3"
    expose:
      - "8080"

  client_c:
    build:
      context: ./Client
      dockerfile: Dockerfile
    depends_on:
      - "mirror"
